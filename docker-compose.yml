version: "3.9"

services:

  backend:
    build: ./backend
    container_name: aiops-backend
    ports:
      - "8080:8080"

  frontend:
    build: ./frontend
    container_name: aiops-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend